{
  "name": "focus-anchor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/newtab.js"
  }
}
